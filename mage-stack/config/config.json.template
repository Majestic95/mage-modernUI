{
  "_comment": "Copy this file to config.json and edit per your machine. config.json is gitignored.",

  "tunnel": {
    "name": "modern-mage",
    "hostname": "modern-mage.com",
    "credentialsFile": "F:\\xmage\\.secrets\\modern-mage-tunnel.json",
    "configFile": "F:\\xmage\\mage-stack\\config\\cloudflared-config.yml"
  },

  "webapi": {
    "_jarHint": "WebApi launches via java.exe with a classpath file (NOT mvn exec:java) so NSSM can PID-track a single JVM. The classpath file is generated at install time by `mvn dependency:build-classpath` and refreshed on every `mage redeploy`.",
    "javaHome": "C:\\Program Files\\Eclipse Adoptium\\jdk-17.0.12.7-hotspot",
    "mainClass": "mage.webapi.WebApiMain",
    "classpathFile": "F:\\xmage\\mage-stack\\config\\classpath.txt",
    "jvmArgsFile": "F:\\xmage\\mage-stack\\config\\jvm-args.txt",
    "workingDirectory": "F:\\xmage\\Mage.Server.WebApi",
    "port": 18080,
    "profile": "prod",
    "_corsOriginsHint": "List EVERY hostname from which the browser will fetch the API. Vercel's default redirects apex<->www when both are configured; if either is published, BOTH must appear here or browsers landing at the redirected host get a 'Can't reach the server' error from a CORS preflight that returns 200 without an Access-Control-Allow-Origin header. Lesson learned 2026-05-06; see ADR 0013 + reference_cloudflare_tunnel_gotchas.md memory.",
    "corsOrigins": "https://xmage-playtest.vercel.app,https://modern-mage.com,https://www.modern-mage.com,http://localhost:5173,http://localhost:4173",
    "registrationEnabled": "true",
    "adminPasswordSource": "env"
  },

  "watchdog": {
    "healthUrl": "http://localhost:18080/api/health",
    "intervalSeconds": 30,
    "timeoutSeconds": 5,
    "consecutiveFailuresBeforeRestart": 3
  },

  "services": {
    "webapi": "MageWebApi",
    "tunnel": "MageTunnel",
    "watchdog": "MageWatchdog"
  },

  "logs": {
    "directory": "F:\\xmage\\mage-stack\\logs",
    "rotationMaxBytes": 10485760,
    "rotationMaxFiles": 5
  }
}
